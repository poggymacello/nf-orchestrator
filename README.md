# nf-orchestrator

Under construction — full pitch, architecture diagram, and one-command
quickstart land in milestone M7. For now, see `pyproject.toml` and
`orchestrator/` for the current state of the code.
